const inputTypes = {
  INPUT: 'input',
  TEXT_AREA: 'textarea',
  SELECT: 'select',
};

const inputSubTypes = {
  TEXT: 'text',
  EMAIL: 'email',
};

export default inputTypes;

export {
  inputTypes,
  inputSubTypes,
}