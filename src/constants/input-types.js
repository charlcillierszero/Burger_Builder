const inputTypes = {
  INPUT: 'input',
  TEXT_AREA: 'textarea',
  SELECT: 'select',
};

const inputSubTypes = {
  TEXT: 'text',
  EMAIL: 'email',
  PASSWORD: 'password',
};

export default inputTypes;

export {
  inputTypes,
  inputSubTypes,
}